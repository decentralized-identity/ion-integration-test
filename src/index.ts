// Import the config which contains what tests to run
// Execute them with desired retry interval (blockchain anchor time) and timeout (after how long should the test be a failure)

console.log('Hello world');